Use case: background-extraction. Edit target: the attached TWO-pose Kaida pixel-art sprite sheet. Change ONLY the background: remove the entire gray checkerboard and the faint gray artifacts drawn over it, replacing them with genuinely transparent alpha. The checkerboard in this input is baked into the image; it is not transparency. Return a PNG with a real alpha channel and alpha=0 outside the two characters. Preserve BOTH character drawings exactly, including their original pixels, scale, position, feet, hair, clothing, sword, pose differences and the two equal 768-pixel-wide cells on the existing 1536x1024 canvas. Do not redraw or refine Kaida. Do not resize, recenter or crop. No solid background, no displayed checkerboard, no texture, no shadow, no grid, no guides, no text. This is strictly background removal of an existing two-frame sprite sheet, not a new generation.
