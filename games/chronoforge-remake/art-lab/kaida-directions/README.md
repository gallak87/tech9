# Kaida direction study

Open [the comparison page](../kaida-guided-idle/?hero=kaida). Front, back and right standing views were generated together from the approved idle reference. These are static comparison poses, not new animation loops.

`candidate.png` is the unmodified generated sheet. `study.json` declares source crops, visible bounds and the intended body centers for consistent display. The model painted a checkerboard; alpha is fully opaque. The page presents this honestly as a raw study, with A/B comparison instead of a transparent overlay. Existing game assets remain active.

See `prompt.txt` and `generation.json` for the exact built-in imagegen request and provenance.
