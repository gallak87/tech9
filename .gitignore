node_modules/
.DS_Store

# Generated per machine by `npm run mcp:setup` — contains absolute paths to
# the Godot binary and the godot-mcp checkout, so it is not portable.
.mcp.json

# game build output + review captures
games/*/shots/*
!games/*/shots/proto-ref/
games/*/dist/
