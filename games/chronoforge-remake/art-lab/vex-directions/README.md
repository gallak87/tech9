# Vex direction study

Open [the comparison page](../kaida-guided-idle/?hero=vex). Front, back and right standing views were generated together from the approved idle reference in one call. These are static comparison poses, not new animation loops.

`candidate.png` is the unmodified generated sheet. `study.json` declares source crops, visible bounds and intended body centers for consistent display. The model painted a checkerboard; the source has no alpha channel. The page presents the raw study with A/B comparison instead of a transparent overlay. Existing game assets remain active.

See `prompt.txt` and `generation.json` for the exact built-in imagegen request and provenance.
