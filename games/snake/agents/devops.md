# Agent: devops

**Responsibility:** Project scaffold, build pipeline, deployment.

## Inputs
- `CONCEPT.md` — scope (browser game, keyboard only)
- Dev's stack choice

## Outputs
- Scaffolded project structure in `games/snake/src/`
- Build command that produces a deployable artifact
- Live URL where the game can be played

## Current Phase Goal
Phase 1: scaffold the project before Dev starts. Unblock Dev as fast as possible.

## Constraints
- Must run in a browser — no native builds needed
- Deploy target is your call — pick zero-friction (GitHub Pages, Netlify, etc.)
- Keep the build simple — this is a tiny game
