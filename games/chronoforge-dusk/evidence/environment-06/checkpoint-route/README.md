# Route and launch checkpoint

Kaida a1 baseline from `21497f447e02` (a1 graduation `b246ff1`). New launch dispatch keeps the original native tests on the development scene. Exploration uses the same actor, asset assembly and accepted movement tuning, with independent authored camera framing. F2 switches between coast and development; normal exploration has pause/resume/quit and a safe reset.

Exported Mac checks: 36 environment checks + 6 fresh-process checks; existing Kaida gate 61 + 6; native helper integrity 4. Full route walking and reverse running used mapped physical key events through `Input.parse_input_event`, including ramps, side overlook, boundary corner, return, pause and reset. Accepted tuning unchanged. Exact tested source digests are in each report. These are structural blockout checks, **not finished-art or final performance acceptance**. Scenery production ran alongside parts of the Kaida check; use final environment measurements for performance.
